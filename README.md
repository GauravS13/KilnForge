# KilnForge

> Filled in incrementally as the project is built, not backfilled at the end — sections below are placeholders until their corresponding commit lands.

Resize, rotate, watermark, and archive images over HTTP — replacing `sharp` and `tar` with Bun's own native `Bun.Image` and `Bun.Archive` APIs. Zero third-party runtime dependencies.

## Quickstart

_(filled in C5 — one-command build/run, curl examples for every endpoint)_

## Architecture

_(filled in C5 — multi-file layout rationale, why the Single File bonus was explicitly not claimed)_

## Endpoints

_(filled in C5)_

## Parity with `sharp` and `tar`

_(filled in C5 — feature parity matrix, migration guide)_

## Limits

_(filled in C5 — documented gaps, honestly)_

## Demo

_(filled in C5 — five-minute demo script)_

## Development

Requires Bun `1.4.0` or later. See `.bun-version`.

```
bun install
bun run verify   # Foundation Verification Harness — run this first
bun test
```

See `TASKS.md` for what every `bun run` script does and why.
